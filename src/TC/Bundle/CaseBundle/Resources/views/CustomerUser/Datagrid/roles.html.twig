{% set roles = record.getValue('roles') %}
{% if roles %}
    {% for role in roles %}
        {% if not loop.last %}
            {{ role.label ~ ','}}
        {% else %}
            {{ role.label}}
        {% endif %}
    {% endfor %}
{% endif %}
