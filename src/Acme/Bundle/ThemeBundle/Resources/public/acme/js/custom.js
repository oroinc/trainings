/* Acme custom theme script loaded via Subresource Integrity (SRI). */
(function() {
    'use strict';

    document.addEventListener('DOMContentLoaded', function() {
        if (window.console && typeof window.console.info === 'function') {
            window.console.info('[AcmeThemeBundle] custom.js loaded with SRI hash verification.');
        }
    });
})();
